#include <stdio.h>

int main(){     
printf("Enter an integer number:");
scanf("%d",&number);
if(number < 100)
  }
printf(" your number is smaller than a 100");
}else
{
printf("your number contains more than two digits");
}
return 0;
}
